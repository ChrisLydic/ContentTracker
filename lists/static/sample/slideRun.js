$( document ).ready( function () {
    slideShifter.run( {
        // set transition for all slides
        transition: 'slideUp',
        // set interval between transitions, in seconds
        interval: 4,
    } );
});